<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_task_form2" model="ir.ui.view">
        <field name="name">project.task.form</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_form2"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='date_deadline']" position="after">
                <field name="task_category_id" options="{'no_create': True}"/>
            </xpath>
        </field>
    </record>

    <record id="view_task_search_form" model="ir.ui.view">
        <field name="name">project.task.search.form</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_search_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='stage_id']" position="after">
                <field name="task_category_id"/>
            </xpath>
            <xpath expr="//filter[@name='stage']" position="before">
                <filter string="Category" name="groupby_task_category_id" context="{'group_by': 'task_category_id'}"/>
            </xpath>
        </field>
    </record>

    <record id="quick_create_task_form" model="ir.ui.view">
        <field name="name">project.task.form.quick_create</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.quick_create_task_form"/>
        <field name="arch" type="xml">
            <!-- Add those fields to trigger the onchange and fill task_category_id when creating tasks from the kanban-->
            <xpath expr="//field[@name='name']" position="after">
                <field name="project_id" invisible="1"/>
                <field name="task_category_id" invisible="1"/>
            </xpath>
        </field>
    </record>

</odoo>
